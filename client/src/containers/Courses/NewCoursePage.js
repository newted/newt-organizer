import React from "react";
// Components
import { MainContainer } from "../../components/PageContainers";

const NewCoursePage = props => (
  <MainContainer>{JSON.stringify(props)}</MainContainer>
);

export default NewCoursePage;
